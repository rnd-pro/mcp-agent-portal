export default `
pg-theme-editor-panel {
  display: block;
  min-width: 0;
  height: 100%;
  background: var(--sn-panel-bg);
  color: var(--sn-text);
  font-family: var(--sn-font);
}

pg-theme-editor-panel > cascade-theme-editor {
  height: 100%;
}
`;
