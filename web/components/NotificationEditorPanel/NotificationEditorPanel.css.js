export default `
pg-notification-editor-panel {
  display: block;
  min-width: 0;
  height: 100%;
  background: var(--sn-sys-surface-panel);
  color: var(--sn-sys-on-surface);
  font-family: var(--sn-font);
}

pg-notification-editor-panel > notification-editor {
  height: 100%;
}
`;
