# iso/ — Isomorphic Utilities

Shared code that runs in **both** Node.js and browser.

Import boundary:
- `iso/` imports from `iso/` only
- `node/` imports from `iso/` and `node/`
- `ui/` (web/) imports from `iso/` only

No files yet — utilities will emerge as the codebase grows.
